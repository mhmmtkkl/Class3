package OverLoadingvsOverriding;

public class OverloadingVsOverridingg {

	/*
	 * overLoading 										vs                  						overRiding
	 * 
	 * in the same class															we have one parent at least one child class
	 * 
	 * overloading method should be same name 										overriding method should be same name 										
	 * 
	 * methods should have different parameter ytpe or count 						is should have same parameter type 
	 * 
	 * must have a different return type 											must have a same return type
	 * 
	 * Static methods can be overloaded 											static methods can not be overridden 
	 *
	 *
	 */
}
